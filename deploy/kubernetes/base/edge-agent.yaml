apiVersion: apps/v1
kind: Deployment
metadata:
  name: edge-agent
  namespace: edge-evidence
  labels: {app.kubernetes.io/name: edge-agent, app.kubernetes.io/part-of: edge-evidence}
spec:
  replicas: 1
  selector:
    matchLabels: {app.kubernetes.io/name: edge-agent}
  template:
    metadata:
      labels: {app.kubernetes.io/name: edge-agent, app.kubernetes.io/part-of: edge-evidence}
    spec:
      securityContext:
        runAsNonRoot: true
        seccompProfile: {type: RuntimeDefault}
      containers:
        - name: edge-agent
          image: edge-evidence/edge-agent:local
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8081
          env:
            - name: ALLOWED_ORIGINS
              valueFrom:
                configMapKeyRef:
                  name: edge-evidence-config
                  key: ALLOWED_ORIGINS
            - name: EDGE_AGENT_ENABLE_ADK
              valueFrom:
                configMapKeyRef:
                  name: edge-evidence-config
                  key: EDGE_AGENT_ENABLE_ADK
          readinessProbe:
            httpGet: {path: /healthz, port: http}
            initialDelaySeconds: 2
            periodSeconds: 5
          livenessProbe:
            httpGet: {path: /healthz, port: http}
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests: {cpu: 50m, memory: 64Mi}
            limits: {cpu: 500m, memory: 256Mi}
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: {drop: [ALL]}
---
apiVersion: v1
kind: Service
metadata:
  name: edge-agent
  namespace: edge-evidence
spec:
  selector: {app.kubernetes.io/name: edge-agent}
  ports:
    - name: http
      port: 8081
      targetPort: http
