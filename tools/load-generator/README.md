# Load generator

Owner: bounded synthetic load tooling.

The tool depends only on the shared HTTP client contract and emits a deterministic summary. It does not import Evidence API implementation code. Build from the repository root with `docker build -f tools/load-generator/Dockerfile .`.
