# Shared contracts

This package contains only stable Pydantic models, component-manifest parsing, and small HTTP clients. It does not contain service state, FastAPI applications, agent reasoning, templates, deployment logic, or worker implementations.
